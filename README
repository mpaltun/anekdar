run:    make & make start
        go to localhost:9999

subscribe   : GET   /sub/{channel}
publish     : POST  /pub/{channel} [message_body]

Multiple channel supported.

publish example :
    curl -H "Content-Type: application/json" -X POST -d '{"realtime":["publish", "subscribe"], "server" : "anekdar"}' http://localhost:9999/pub/channel_1/
